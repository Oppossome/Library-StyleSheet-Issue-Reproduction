@using Sandbox
@inherits PanelComponent
@attribute [Title("Minimal Library StyleSheet Reproduction")]

<root>
</root>

@code {
	protected override void OnEnabled() =>
		Panel.StyleSheet.Load( "Minimal.scss" );
}
